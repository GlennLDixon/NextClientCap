import React from "react"

export const TBLandingCard = ({ }) => {
    return (
        <div className="card">
            <div className="card-content">
                <h4>Hello</h4>
                <button type="button" onClick={() => handleDeleteCard(card.id)}>X</button>
            </div>
        </div>
    )
}